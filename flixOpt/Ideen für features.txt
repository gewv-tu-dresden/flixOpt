useextremeValues = True funktioniert nicht bei Aggregation!

high prio:

glpk bringt anderes ergebnis im example!!!

middle prio:

costPerFlowHours, val_rel, min_rel, max_rel etc. als segmentierte Zeitreihen eingebbar machen.
timeseries-Variables!
etc.
einfachere VAriante für sumOn-Bedingungen umsetzen (siehe Peter)
einfaches 1/n für agg_type funktioniert noch nichts so dolle!
TSAM möglichst noch rausziehen aus flixOpt!!!
rename example py-file: "model_and_solve" instead of "preprocessing"!


In yaml steht:
        Speicher:
        - 'var     invest.used_capacity_inFlowHours: len=1 min = 0, max = 1000'
        - 'var     invest.investCosts_segmented_costs: len=1 min = 0, max = None'
        - 'var     invest.investCosts_segmented_PE: len=1 min = 0, max = None'
--> invest unter-gliedern wie bei eqs



cBus ist keine "Komponente", sondern eigtl. separat ODER aber wir bauen so, dass auch Komponente an Komponente sein kann (ohne Bus dazwischen)
